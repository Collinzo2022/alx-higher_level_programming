#!/usr/bin/node

// Create an aray with the three strings
const messages = ['C is fun', 'Python is cool', 'Javascript is amazing'];

// loop through the array
for (const message of messages) {
  console.log(message);
}
